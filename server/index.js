const express = require("express");
// import express
const app = express();
// run it throught the const app

app.listen(5000, () => {
  // if the server starts
  console.log("server has started on port 5000");
  //   show this in the console log
});
